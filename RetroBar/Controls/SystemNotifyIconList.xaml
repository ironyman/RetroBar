<UserControl x:Class="RetroBar.Controls.SystemNotifyIconList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RetroBar.Controls"
             xmlns:converters="clr-namespace:RetroBar.Converters"
             xmlns:dd="urn:gong-wpf-dragdrop"
             Loaded="SystemNotifyIconList_Loaded"
             Unloaded="SystemNotifyIconList_Unloaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:IconListOrientation x:Key="iconsOrientation" />
        </ResourceDictionary>
    </UserControl.Resources>
    <ItemsControl x:Name="SystemIcons"
                  Focusable="False"
                  Style="{DynamicResource NotifyIconItems}"
                  dd:DragDrop.IsDropTarget="True"
                  dd:DragDrop.DropHandler="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:SystemNotifyIconList}}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel>
                    <WrapPanel.Orientation>
                        <MultiBinding Converter="{StaticResource iconsOrientation}">
                            <Binding Path="Orientation"
                                     RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=Window}" />
                            <Binding Path="Rows"
                                     RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=Window}" />
                        </MultiBinding>
                    </WrapPanel.Orientation>
                </WrapPanel>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <ItemContainerTemplate>
                <local:NotifyIcon Host="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" />
            </ItemContainerTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
